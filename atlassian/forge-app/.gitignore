# Build metadata (generated at build time)
tools/.build_meta.*

# UI build metadata (generated at build time from .build_meta.json)
src/gadget-ui/src/ui_build_meta.ts

# Local-only proof artifacts (must not affect determinism)
proof_artifacts/
proof_run_*.sh
proof_phase*.sh
proof_run_phases_*.sh
proof_run_basic.sh
*.proof.log
e2e/.auth/
# Playwright auth state (local only, not tracked)
tests/playwright/.auth/state.json
tests/playwright/.auth/*.json
# Backbone fix proof documents (for cleanup/evidence only, not tracked)
BACKBONE_SNAPSHOT_FIX_COMPLETE.md# Playwright test artifacts (deterministic proofs)
test-results/
playwright-report/
blob-report/
.last-run.json
# Playwright test artifacts (deterministic proofs)
test-results/
playwright-report/
blob-report/
.last-run.json
